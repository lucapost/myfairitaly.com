---
layout: gallery
title: Gadgets
lang: en
---

Galleria italiana

{% if gadgets == empty %}
	<p>empty!</p>
{% else %}
	{% for marker in page.gadgets %}
		<img src="{{ marker.path }}" alt="{{ marker.alt }}" title="{{ marker.title }}" lang="{{ marker.lang }}"/>
	{% endfor %}
{% endif %}
